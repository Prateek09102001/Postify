<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Edit Post</title>
</head>
<body class="bg-zinc-900 text-white flex items-center justify-center min-h-screen">

    <div class="w-full max-w-2xl bg-zinc-800/80 backdrop-blur-md p-8 rounded-lg shadow-lg border border-zinc-700">
        <!-- Logout Button -->
        <div class="flex justify-end">
            <a class="bg-red-500 hover:bg-red-600 transition px-4 py-2 rounded-md text-sm font-medium shadow-md" href="/logout">Logout</a>
        </div>

        <!-- Page Title -->
        <h2 class="text-3xl font-semibold text-center mt-3">Edit Your Post</h2>
        <p class="text-zinc-400 text-center mb-5">Update your thoughts below 👇</p>

        <!-- Edit Post Form -->
        <form method="post" action="/update/<%= post._id %>" class="space-y-4">
            <textarea 
                name="content" 
                class="p-3 w-full h-40 outline-none resize-none bg-transparent border-2 border-zinc-700 rounded-md focus:ring-2 focus:ring-blue-500 transition text-lg shadow-inner"
                placeholder="What's on your mind?"
                required><%= post.content %></textarea>

            <!-- Submit Button -->
            <input 
                type="submit" 
                value="Update Post" 
                class="w-full px-5 py-3 text-lg font-medium bg-yellow-500 hover:bg-yellow-600 transition rounded-md shadow-md cursor-pointer">
        </form>
    </div>

</body>
</html>
